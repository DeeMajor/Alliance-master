@model IEnumerable<Accommodation.Models.CleanerRoaster>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.GetCleaners.FullName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GetService.Description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.GetCleaners.FullName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GetService.Description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CleanerRoasterId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CleanerRoasterId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CleanerRoasterId })
        </td>
    </tr>
}

</table>
